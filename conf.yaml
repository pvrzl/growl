growl:
  database:
    driver: mysql # required
    # url: root:@/
    url: root:@/ # required
    name: "growl_test?charset=utf8&parseTime=True&loc=Local&sql_mode='ALLOW_INVALID_DATES'" # required | recommended flag : 
    prefix:  
    singulartable: false # default : false
  redis:
    host: localhost # default : localhost
    port: "6379" # default : 6379
    password: #default : ""
    channel: "channel"
    enable: true
  misc:
    localcache: true
    log: false
    flushatinit: true
    defaultcacheduration: 1
    defaultcachedurationunit: hour # enum hour|minute|second
